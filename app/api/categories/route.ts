import { NextRequest, NextResponse } from 'next/server';
import { supabaseServer } from '@/lib/supabase';
export async function GET(){try{const s=supabaseServer();const {data,error}=await s.from('categories').select('*').order('is_active',{ascending:false}).order('type').order('name');if(error)throw error;return NextResponse.json({categories:data??[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:500})}}
export async function POST(req:NextRequest){try{const b=await req.json(),name=String(b.name??'').trim(),type=String(b.type??'expense');if(!name)return NextResponse.json({error:'Category name is required.'},{status:400});if(!['income','expense'].includes(type))return NextResponse.json({error:'Invalid category type.'},{status:400});const s=supabaseServer();const {data,error}=await s.from('categories').insert({name,type,description:String(b.description??'').trim()||null,is_active:b.is_active!==false}).select('*').single();if(error)throw error;return NextResponse.json({category:data},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:500})}}