import { NextRequest, NextResponse } from 'next/server';
import { supabaseServer } from '@/lib/supabase';
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const id=(await params).id,b=await req.json(),patch:any={};for(const k of ['name','type','description','is_active'])if(k in b)patch[k]=b[k];if('name'in patch&&!String(patch.name).trim())return NextResponse.json({error:'Category name is required.'},{status:400});if('type'in patch&&!['income','expense'].includes(String(patch.type)))return NextResponse.json({error:'Invalid category type.'},{status:400});const s=supabaseServer();const {data,error}=await s.from('categories').update(patch).eq('id',id).select('*').single();if(error)throw error;return NextResponse.json({category:data})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:500})}}
export async function DELETE(_:NextRequest,{params}:{params:Promise<{id:string}>}){try{const id=(await params).id,s=supabaseServer();const {error}=await s.from('categories').delete().eq('id',id);if(error)throw error;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Delete failed. If this category is used by transactions, deactivate it instead.'},{status:400})}}